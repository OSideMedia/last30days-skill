`.env` lines written with the shell's `export` prefix (`export KEY=value`) now set `KEY`; before, they created a key literally named `export KEY` and the setting was silently ignored. The setup wizard reads keys the same way, so it no longer appends a second copy of a key that is already set with `export`.
